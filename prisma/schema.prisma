// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // Si más adelante usas múltiples esquemas de Postgres:
  // schemas = ["public"]
}

/*
  🔹 De momento no declaramos modelos.
  🔹 Esto es suficiente para que `prisma generate` funcione.
  🔹 Cuando quieras mapear tablas existentes de Supabase o crear nuevas,
     agrega aquí tus `model`, `enum`, etc., y corre `prisma migrate` o `db push`.
*/
